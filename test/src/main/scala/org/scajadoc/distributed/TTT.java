package org.scajadoc.distributed;

/**
 * // TODO: Document this
 *
 * @author Filip
 * @since 4.0
 */
public class TTT {

   byte addOneAndOne() {
       byte a = 1;
       byte b = 1;
       byte c = (byte) (a + b);
       return c;
   }
}
